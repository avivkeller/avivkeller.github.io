---
const {
  background = "bg-white",
  padding = "py-20",
  class: className = "",
  ...props
} = Astro.props;
---

<section class={`${background} ${padding} ${className}`} {...props}>
  <div class="container mx-auto px-4">
    <slot />
  </div>
</section>
