---
import { Icon as IconComp } from "astro-icon/components";

const { name, size = 24, class: className = "", ...props } = Astro.props;
---

<IconComp name={name} size={size} class={className} {...props} />
